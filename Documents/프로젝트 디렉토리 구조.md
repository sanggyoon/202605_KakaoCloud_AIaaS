# KakaoCloud_Project 디렉토리 구조

## 전체 흐름 요약

Ansible로 k3s 클러스터를 프로비저닝하고, GitHub Actions로 Docker 이미지를 빌드·푸시하며, ArgoCD GitOps로 Supabase·FE·모니터링 등 전체 서비스를 자동 배포·관리하는 구조.

```
개발자 git push
  → GitHub Actions (CI): 빌드 → GHCR 이미지 푸시 → manifest 태그 업데이트
  → ArgoCD (CD): Git 변경 감지 → k3s 클러스터 자동 sync
```

---

## 전체 트리

```
KakaoCloud_Project/
├── .gitignore
├── readme.md
├── .github/
│   └── workflows/
│       └── deploy-4k-fe.yml            (GitHub Actions CI/CD)
├── 4K_FE/                              (Frontend — Next.js)
│   ├── Dockerfile                      (multi-stage, standalone 모드)
│   ├── .dockerignore
│   ├── next.config.ts                  (output: standalone)
│   ├── package.json
│   ├── tsconfig.json
│   └── app/
│       ├── layout.tsx
│       ├── globals.css
│       ├── page.tsx                    (온보딩/랜딩 페이지)
│       ├── dashboard/
│       │   └── page.tsx                (메인 대시보드 — Supabase fetch, 필터, 추천)
│       ├── components/
│       │   ├── PosterCard.tsx          (포스터 카드, 3D tilt 인터랙션)
│       │   ├── DetailOverlay.tsx       (영화 상세 오버레이 — YouTube 트레일러 embed)
│       │   ├── FilterBar.tsx           (연도·장르·상황 필터 UI)
│       │   ├── RandomModal.tsx         (랜덤 추천 모달)
│       │   ├── MiniGraph.tsx           (미니 클라이맥스 그래프 — Tutorial에서 사용)
│       │   └── Tutorial.tsx            (최초 방문자 온보딩 튜토리얼)
│       └── lib/
│           └── data.ts                 (Movie 타입, Supabase 상수, 필터 유틸, localStorage 헬퍼)
├── 4K_BE/                              (Backend 스크립트)
│   ├── requirements.txt                (Python 의존성: httpx, python-dotenv)
│   ├── .python-version
│   └── DB_SCRIPTS/
│       ├── create_service_tables.sql   (movies, movie_vectors 테이블 DDL + pgvector)
│       ├── seed_movies.py              (TMDB API → data.movies 테이블 upsert 스크립트)
│       └── .env                        (TMDB_API_KEY, DATA_SUPABASE_KEY 등 — gitignore)
├── Documents/                          (문서)
│   ├── 프로젝트 디렉토리 구조.md
│   ├── 인프라 구축 기록.md
│   ├── 인프라 구축 방법(명령어).md
│   └── CICD 전략.md
└── Ansible/
    ├── ansible.cfg
    ├── inventory.ini
    ├── ssh_config
    ├── kubeconfig                      (gitignore)
    ├── k3s_node_token                  (gitignore)
    ├── group_vars/
    │   └── all.yaml
    ├── playbooks/
    │   ├── 00-ping.yml
    │   ├── 01-common.yml
    │   ├── 02-k3s-server.yml
    │   ├── 03-k3s-agent.yml
    │   └── 04-longhorn-prereq.yml
    ├── helm-values/                    (Ansible 초기 설치용 — 1회성)
    │   ├── ingress-nginx.yaml
    │   └── longhorn.yaml
    ├── values/                         (ArgoCD가 매 sync마다 참조하는 Helm values)
    │   ├── values-argocd.yaml
    │   ├── values-ai.yaml
    │   ├── values-data.yaml
    │   ├── values-prometheus.yaml
    │   ├── values-loki.yaml
    │   ├── values-promtail.yaml
    │   └── values-redis.yaml
    ├── generate-apply-secrets/
    │   ├── generate-secrets.sh
    │   ├── secrets-data/               (gitignore)
    │   │   ├── .env
    │   │   └── apply-secrets.sh
    │   └── secrets-ai/                 (gitignore)
    │       ├── .env
    │       └── apply-secrets.sh
    └── manifests/
        ├── apps/                       (ArgoCD infra-manifests가 sync)
        │   ├── cluster-issuer.yaml
        │   ├── ingress-nginx-config.yaml
        │   └── ingress-studio.yaml     (Studio + Kong API Ingress 정의)
        ├── argocd/                     (수동 kubectl apply — 최초 1회)
        │   ├── argocd-app-infra.yaml
        │   ├── argocd-app-supabase-ai.yaml
        │   ├── argocd-app-supabase-data.yaml
        │   └── argocd-app-4k-cinema.yaml
        ├── 4k-cinema/                  (ArgoCD 4k-cinema App이 sync — Kustomize)
        │   ├── kustomization.yaml      (이미지 태그 관리 — CI가 자동 업데이트)
        │   ├── deployment.yaml
        │   ├── service.yaml
        │   └── ingress.yaml
        └── test/
            ├── gpu-test.yaml
            └── test-nginx.yaml
```

---

## `.github/` — GitHub Actions CI

| 하위 경로 | 역할 |
|---|---|
| `workflows/deploy-4k-fe.yml` | `4K_FE/**` 변경 시 Docker 빌드 → GHCR 푸시 → `Ansible/manifests/4k-cinema/kustomization.yaml` 이미지 태그 자동 업데이트 |

---

## `4K_FE/` — 프론트엔드 (Next.js)

4K Cinema 영화 추천 서비스의 프론트엔드. App Router 기반, Supabase에서 실시간 데이터를 fetch해 표시.

| 파일/경로 | 역할 |
|---|---|
| `Dockerfile` | multi-stage 빌드. deps → builder → runner(standalone) 3단계로 이미지 최소화 |
| `.dockerignore` | node_modules, .next, .env 등 빌드 불필요 파일 제외 |
| `next.config.ts` | `output: 'standalone'` 설정 |
| `app/page.tsx` | 온보딩/랜딩 페이지 |
| `app/dashboard/page.tsx` | 메인 대시보드. Supabase REST API에서 영화 목록 fetch, 연도·장르 필터, 최근 본 영화(localStorage) 섹션 |
| `app/lib/data.ts` | `Movie` 인터페이스, `Filters` 타입, Supabase URL/anon key 상수, `posterUrl()`·`genreList()`·`castList()` 유틸, `getRecentIds()`·`addRecentId()` localStorage 헬퍼 |
| `app/components/PosterCard.tsx` | TMDB CDN 포스터 이미지, 3D tilt 인터랙션, glare 효과 |
| `app/components/DetailOverlay.tsx` | 영화 상세 오버레이. overview, 감독, 배우, YouTube 트레일러 iframe embed |
| `app/components/FilterBar.tsx` | 연도 슬라이더, 장르·상황 칩 필터 |
| `app/components/RandomModal.tsx` | 랜덤 추천 모달. 전달받은 `movies[]`에서 무작위 선택 + 셔플 애니메이션 |
| `app/components/MiniGraph.tsx` | SVG 기반 미니 클라이맥스 그래프 (Tutorial 데모용) |
| `app/components/Tutorial.tsx` | 최초 방문자 온보딩 (4단계, localStorage로 완료 여부 관리) |

---

## `4K_BE/` — 백엔드 스크립트

현재는 DB 초기화·시드 스크립트만 있으며, FastAPI 서버는 미구현.

| 파일/경로 | 역할 |
|---|---|
| `requirements.txt` | Python 의존성 (`httpx`, `python-dotenv`) |
| `DB_SCRIPTS/create_service_tables.sql` | `public.movies`·`public.movie_vectors` 테이블 DDL. pgvector extension, BIGSERIAL PK, tmdb_id UNIQUE 인덱스 |
| `DB_SCRIPTS/seed_movies.py` | TMDB API에서 403편 메타데이터(제목·포스터·감독·배우·장르·트레일러 등) 를 가져와 Supabase `data` ns movies 테이블에 upsert. 배치 50개 단위, 중복 skip, 첫 1개 insert 후 DB 확인 후 진행 |
| `DB_SCRIPTS/.env` | `TMDB_API_KEY`, `DATA_SUPABASE_URL`, `DATA_SUPABASE_KEY` 보관 (gitignore) |

---

## `Ansible/` — 인프라 프로비저닝 + K8s 매니페스트

k3s 클러스터 구성·운영을 위한 Ansible 자동화 코드와 ArgoCD가 관리하는 모든 K8s 매니페스트를 함께 관리.

| 하위 경로 | 역할 |
|---|---|
| `playbooks/` | 클러스터 초기 셋업 플레이북. `00` ping → `01` 공통 설정(swap 비활성화, 커널 모듈) → `02` k3s master → `03` k3s agent → `04` Longhorn 전제조건 순서로 실행 |
| `manifests/apps/` | ArgoCD `infra-manifests` Application이 sync. cert-manager ClusterIssuer, ingress-nginx ConfigMap, Studio/API Ingress |
| `manifests/4k-cinema/` | ArgoCD `4k-cinema` Application이 sync. Kustomize 기반 FE 앱 매니페스트. CI가 `kustomization.yaml`의 `newTag`를 git SHA로 자동 갱신 |
| `manifests/argocd/` | ArgoCD Application 오브젝트 정의. 최초 1회 `kubectl apply`로 등록 |
| `manifests/test/` | GPU 동작 확인, nginx 테스트용 임시 매니페스트 |
| `helm-values/` | ingress-nginx, Longhorn Helm 초기 설치에 사용 (1회성) |
| `generate-apply-secrets/` | AI/Data Supabase의 k3s Secret을 `.env`에서 생성·적용하는 쉘 스크립트 |
| `group_vars/all.yaml` | 모든 호스트에 공통 적용되는 Ansible 변수 |
| `inventory.ini` | 관리 대상 노드 목록 (master/agent 구분) |
| `kubeconfig`, `k3s_node_token`, `ssh_config` | 클러스터 접속 자격증명 및 SSH 설정 (gitignore 대상) |

---

## `Ansible/values/` — Helm values 파일 (ArgoCD GitOps)

ArgoCD가 Git에서 직접 읽는 각 서비스의 Helm values. `helm-values/`(초기 1회 설치용)와 구분되며, 이 디렉토리는 ArgoCD가 매 sync마다 참조한다.

| 파일 | 역할 |
|---|---|
| `values-argocd.yaml` | ArgoCD 자체 설정 (ingress, insecure 모드 등) |
| `values-ai.yaml` | Supabase AI ns 설정 (db, studio, kong, storage, minio 활성) |
| `values-data.yaml` | Supabase data ns 설정 (db, studio, kong, rest, meta 활성) |
| `values-prometheus.yaml` | Prometheus + Grafana + Alertmanager 설정 |
| `values-loki.yaml` | Loki 로그 저장소 설정 |
| `values-promtail.yaml` | Promtail 로그 수집 에이전트 설정 |
| `values-redis.yaml` | Redis 캐시 설정 |

---

## `Ansible/manifests/apps/ingress-studio.yaml` — Ingress 구조

한 파일에 여러 Ingress가 정의됨. ArgoCD `infra-manifests`가 자동 적용.

| Ingress 이름 | 호스트 | 경로 | 백엔드 | 인증 |
|---|---|---|---|---|
| `supabase-data-studio` | data.4kakao.kro.kr | `/*` | Studio:3000 | Basic Auth |
| `supabase-data-api` | data.4kakao.kro.kr | `/rest/v1`, `/auth/v1`, `/storage/v1` | Kong:8000 | Supabase JWT |
| `supabase-ai-studio` | ai.4kakao.kro.kr | `/*` | Studio:3000 | Basic Auth |
| `grafana` | grafana.4kakao.kro.kr | `/*` | Grafana:80 | Grafana 자체 인증 |
| `argocd` | argocd.4kakao.kro.kr | `/*` | argocd-server:80 | ArgoCD 자체 인증 |

---

## `Documents/` — 문서

| 파일 | 내용 |
|---|---|
| `프로젝트 디렉토리 구조.md` | 이 파일. 전체 파일 구조와 각 경로의 역할 |
| `인프라 구축 기록.md` | 클러스터 구성, 네트워크, 스토리지, 트러블슈팅 이력 |
| `인프라 구축 방법(명령어).md` | 처음부터 재구축할 때 순서대로 실행할 명령어 모음 |
| `CICD 전략.md` | GitHub Actions + ArgoCD 기반 CI/CD 파이프라인 설계 |
